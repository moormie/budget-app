import styled from "styled-components";

const Container = styled.div`
  padding: 24px;
`;
const Spacing = styled.div`
  height: 18px;
`;

const StyledExpenses = {
  Container,
  Spacing,
};

export default StyledExpenses;
